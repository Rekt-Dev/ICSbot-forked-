import React from "react";

const Dashboard = () => {
  return (
    <div>
      <h2>Dashboard</h2>
      {/* Display user portfolio, positions, order history, etc. */}
    </div>
  );
};

export default Dashboard;
